/**
 * Phase 9 Screen Components — Barrel Export
 *
 * Four focused, single-responsibility screen components.
 * Each is a self-contained data-fetching + rendering unit
 * that accepts only a caseId prop and handles its own
 * loading/error/empty states.
 */

export { IntakeScreen } from './IntakeScreen';
export { CorrelationBoard } from './CorrelationBoard';
export { RiskDesk } from './RiskDesk';
export { BriefViewer } from './BriefViewer';
